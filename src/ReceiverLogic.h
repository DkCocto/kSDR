#pragma once

#include "Env.h"
#include "Config.h"

class ReceiverLogic {
/*private:
	int previousWindowWidth;

public:

	bool initialized = false;

	float totalBin;

	float selectedBin;

	float stepX;

	int windowWidth;

	float receiverPosByCoords;
	float receiverPos;
	
	int mode = 0;

	float deltaXPos = 0.0;

	Config* config;

	ReceiverLogic(Config* config, int windowWidth);

	void init(int windowWidth);

	void initAfterResize(int windowWidth);

	float getReseiverPos(int windowWidth, int selectedBin);

	void setAbsoluteXpos(int windowWidth, float pos);

	void storeDeltaXPos(float mouseX);

	int getSelectedFreq();

	//Возвращает ширину фильтра в координатах рисуемого спектра
	float getFilterWidthAbs(int filterWidth);

	//Возвращает ширину фильтра в пикселях
	int getFilterWidthPX(int filterWidth);

	struct ReceiveBinArea {
		int A;
		int B;
	};

	ReceiveBinArea getReceiveBinsArea(int filterWidth, int receiverMode);*/
};